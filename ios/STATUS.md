# Locus iOS — Status Tracker

Live tracker for the iOS build. Updated after every phase. Plan details: `ios/PLAN.md`.

## Phase board

| Phase | Scope | Status | Verified on simulator | Comments |
|---|---|---|---|---|
| 0 | Foundation & design system | Done | 2026-08-16, iPhone 17 Pro (iOS 26) | xcodegen project, LocusTheme, glass components, APIClient/Keychain, auth gate, floating dock shell |
| 1 | Home | Done | 2026-08-16, iPhone 17 Pro (iOS 26) | Live capability cards, stats, quick actions |
| 2 | Ask (core chat) | Done | 2026-08-16, iPhone 17 Pro (iOS 26) | Jobs pipeline, composer, pipeline telemetry card |
| 3 | Ask (streaming + mermaid) | Done | 2026-08-16, iPhone 17 Pro (iOS 26) | NDJSON streaming, WKWebView mermaid + lightbox |
| 4 | Private chats | Done (autopilot review unverified) | 2026-08-16, iPhone 17 Pro (iOS 26) | SSE rooms, share sheet, guests panel, copilot, autopilot |
| 5 | Secret Images | Done | 2026-08-16, iPhone 17 Pro (iOS 26) | Vault grid, viewer, upload/delete |
| 6 | Library | Done | 2026-08-17, iPhone 17 Pro (iOS 26) | Collections, files, upload |
| 7 | Settings | Done (per-model visibility not built) | 2026-08-17, iPhone 17 Pro (iOS 26) | Default model picker, auto-select, health tests, theme, server URL |
| 8 | Polish & release | Pending | — | Haptics pass, global search, splash, TestFlight |

## Rules

- All user-facing strings and docs in English only.
- No backend changes — the app is a pure client of the existing REST/streaming API.
- Every phase must compile and run on the iOS simulator before the next one starts.

## Work log

| Date | Entry |
|---|---|
| 2026-08-16 | Plan written and approved to start; `.playwright-mcp` artifacts deleted and git-ignored (plus Xcode build artifacts). Tracker created. |
| 2026-08-16 | Phase 0 started — project scaffold + design system + networking + shell. |
| 2026-08-16 | Phase 0 **verified on the simulator** (iPhone 17 Pro). First compile of the project surfaced three fixes: (1) `APIClient.unauthorizedHandler` was actor-isolated and assigned from the main actor — build error; now set through `setUnauthorizedHandler(_:)`. (2) The Private tab used `fingerprint`, which is not an SF Symbol — the dock and the empty-state card rendered no glyph; now `touchid`. (3) `boot()` called the protected `/preferences/explore_ai` before resolving the gate, so every signed-out cold start fired a 401; the call now runs only once signed in, plus after login. Acceptance run against a password-gated backend on :8001: gate shown → sign in → 5 tabs → sign out → gate again → cold start with no 401; token survived relaunch (straight to Home, authenticated 200s in the server log). |

| 2026-08-16 | Phase 1 (Home) built and **verified on the simulator**: hero (empty vs. populated copy, greeting by hour), live capability grid, stats row, quick actions, recent files/chats panels, shimmer skeletons, pull-to-refresh. New shared pieces: `Lib/Format.swift` (file size / relative time / greeting ported 1:1 from the web), `SkeletonCard`, `PageScaffold(showsLogo:)`, and `AppState.libraryIntent` / `pendingChatId` for cross-page navigation (Library consumes in Phase 6, Ask in Phase 2). Verified against the live backend: 3 libraries / 0 files / 5 chats, "6 providers · 683 models", "23/684 models responding", "Auto-fallback on" — matching the web. Populated state checked with a temporary uploaded file, since deleted. |

| 2026-08-16 | Phase 2 (Ask core) built and **verified on the simulator** against the live backend. New files: `AskModel` (job create → 1.5s poll → persisted transcript, matching the web's cadence), `FloatingComposer` (reusable; Private reuses it in Phase 4), `PipelineActivityCard`, `ChatListDrawer`, `FilePickerSheet`, `Lib/Markdown.swift` (block parser: headings, tables, lists, quotes, fenced code with copy) and `Lib/PipelineNotes.swift` (1:1 port of `pipelineNotes.js`). Verified end to end: ask → live pipeline card (mode badge, elapsed, LLM hits/searches/tokens, working notes) → persisted answer with markdown + collapsible sources + follow-up chips; expanded developer trace (stage track, progress, telemetry grid, runtime console with live web events); stop mid-answer (job came back `cancelled`); chat drawer open/switch/new; file scoping (picked 1 file → answer cited only that file); `/max` slash prefix flipping the effort dial live; long-press → Copy / Delete from here. **Client sends `web_search: false` deliberately** — the backend's `_effective_web_search` already ORs in search-intent detection and the High/Max-with-no-files rule, so the 40-pattern web heuristic is not duplicated on iOS. Mermaid answers currently render as code blocks; that is Phase 3's job. |

| 2026-08-16 | Phase 3 built and **verified on the simulator**. **Streaming:** `/api/chat/direct-stream` via `URLSession.bytes`, with a `StreamTrace` (request → connect → stream → save) and a typing cursor. The client does **not** re-implement the endpoint's eligibility rules — it tries the stream for Normal effort with no files and falls back to a job on the backend's 422, so `_effective_web_search` stays the single source of truth. Confirmed by absence: the streamed question created no `ChatJob` while a Max-effort one did. **Fixed a Phase 2 parity bug found here:** the file scope defaulted to `nil` ("all files"); the web defaults to `[]`, and only an explicit empty scope triggers the backend's High/Max-with-no-files → web-research rule. Now defaults to `[]`. **Mermaid:** mermaid 11.16.0 vendored to `Resources/mermaid.min.js` (same version the web bundles, so both clients draw identically) and rendered in a `WKWebView` loaded from the bundle — no network, works offline. The page applies note 17's legibility floor (`min(1, max(available/natural, 0.7))`), reflows LR→TD on narrow screens for drawing only (copy/view-source still return the author's diagram), and runs the same auto-quote repair pass as `lib/mermaid.js`. Verified: a CI-pipeline flowchart drew correctly, reflowed top-down in the card, opened in the lightbox at its original LR orientation, and pinch-zoomed. A ```mermaid fence stays a code block until streaming finishes, since a half-arrived diagram cannot parse. |
| 2026-08-16 | Not visually captured: the mid-stream trace/cursor. The default model (gemini-3.5-flash-lite) finishes a long answer in ~2s locally, faster than a screenshot round-trip. The streaming path itself is confirmed working end to end; only the intermediate frame is unphotographed. |

| 2026-08-16 | **Navigation reworked at your request — the bottom dock is gone.** Navigation now lives in a left slide-in `SideMenu` (Workspace: Home / Library with a live count / Ask · Signals: Private / Secret Images · theme toggle · Settings), opened by a single floating transparent glass hamburger. The floating gear was removed since Settings is in the menu, so the only chrome over content is that one button and `LocusMetrics.dockClearance` (108pt) became `bottomClearance` (26pt) — pages gained ~82pt of vertical space and Home now shows 8 capability cards instead of 5. Decision D2 in PLAN.md (5-tab floating dock) is superseded. **Composer:** the scrolling chip row is now one fixed row — an effort dropdown (Normal/High/Max) plus icon-only buttons for file scope (with a count badge) and model/general-knowledge. Nothing scrolls sideways. **Bug caught during verification:** a shell-wide `DragGesture` added for edge-swipe-to-open competed with the pages' scroll views and menus, so an unrelated tap could open the drawer; removed — the hamburger and drag-to-close inside the drawer remain. |

| 2026-08-16 | Phase 4 (Private Chats, host side) built and **verified on the simulator against a real guest**. New files: `PrivateIdentity` (host key in the Keychain, client id in UserDefaults), `PrivateChatsModel`/`View`, `NewRoomSheet`, `RoomModel` (SSE + reconnect, 8s presence heartbeat that doubles as the read cursor, 8s safety-net poll, 1s disappear tick), `RoomView`, `GuestsPanel`, `RoomOptionsSheet`, `CopilotSheet`. Verified: create room → share link → a guest joins and posts → the message arrives over SSE with sender, timestamp and a live countdown → host replies → both expire on schedule (server purge + local tick) → guests panel lists the guest **live** with device/network/screen/activity → copilot returns real drafts. **Three bugs found and fixed here.** (1) The generated host key was two joined UUIDs (73 chars) but the backend caps `host_key` at 64, so *every* room create failed with a 422; the key is now 32 hex chars like the web's, and an over-long stored key is replaced on read. (2) That failure was invisible because the create error was written to a field the 8s refresh then cleared — create failures are now an alert that polling cannot wipe. (3) The composer's send arrow was white-on-white in Bright mode whenever the field was empty. Also: iOS now sends a real `User-Agent`, since the guests panel derives device/OS from it — the host's own row read "Desktop · Unknown OS" before, and now reads "Phone · iOS · Locus app" (the "browser" field is relabelled client-side for Locus clients; no backend change). |
| 2026-08-16 | **Not verified: the autopilot draft review card.** It needs autopilot switched on, a guest message to trigger the server-side worker, and catching the hold window. The polling path (`GET /{token}/autopilot`, host-key authorised, never on the room stream) and the Stop / Send now actions are implemented but unexercised. |

| 2026-08-16 | Phase 5 (Secret Images) built and **verified on the simulator**: status/empty state, upload from Photos, thumbnail grid, full-screen viewer with pinch-zoom, double-tap zoom, drag-to-pan and swipe-down dismiss, delete with confirmation from both the grid's context menu and the viewer. Verified end to end against the live vault: uploaded a photo (server compressed it to 46 KB JPEG), the tile and viewer rendered, deleting it emptied `/api/secret-images` again. Image bytes are fetched through the **authenticated** client and decoded in memory, never handed to an image view as a URL — `/secret-images/view/{id}` is behind the Sign-in Gate and returns a relative path, the same two reasons the web fetches blobs. **Bug fixed during verification:** a `PhotosPicker` nested inside a `Menu` never presents (menu rows must be plain buttons), so "From Photos" did nothing; the picker is now driven by a state flag from a normal button. |
| 2026-08-16 | Not verified in Phase 5: the "From Files" import path (the document picker branch). |

| 2026-08-17 | Phase 6 (Library) built and **verified on the simulator**: collections grid with the web's three folder colours (violet/peach/green swatches taken from `02-pages.css`), create sheet (name, description, colour; create disabled until named), delete library with confirmation, detail sheet with description, file rows (type icon, size, indexing status ported from `embeddingMeta`) and upload from Photos/Files. Verified end to end: created "Phase 6 check" (green) → appeared in the grid → deleted it → `/api/collections` back to the original three; added a document → card count went to "1 file" → row showed "168 B · 1 chunks indexed" → long-press delete → `/api/files` empty again. Indexing state is polled every 3s only while a file is still `pending`/`indexing`, then stops. Accessibility labels added to the two remaining icon-only `GlassCircleButton`s (Ask's chats, the room's back button) to match the new `label:` parameter. **Gap fixed during verification:** the detail sheet had no error surface, so a failed upload just closed the picker silently — it now shows a dismissible inline error, which is how the backend's real "this model does not support image input" rejection became visible. |
| 2026-08-17 | Note: uploading a *photo* to a library fails by design when the default model has no vision support — the extractor reads images through the LLM. Libraries are for documents; the vault (Phase 5) is the place for photos. Not an iOS bug. |
| 2026-08-17 | Not verified in Phase 6: the "From Files" document-picker branch (same untested path as Phase 5). |

| 2026-08-17 | Phase 7 (Settings) built and **verified on the simulator**: default-model card (model chosen, provider *derived and displayed only*, per the model-selection rule), searchable picker grouped by provider with live health tags (latency in ms / "no answer", untested models left untagged), responding-only filter, custom model id, auto-select toggle, dismissible auto-switch note, per-provider "Test" that batches at 40 (the server's `MODEL_TEST_MAX_MODELS`), theme, server URL, sign out. Verified against live data: "23/684 responding" matched Home, provider counts were real (Groq 15 · OpenAI 99 · Gemini 28 · Cerebras 3 · OpenRouter 414 · TokenRouter 125), responding-only cut Groq 15 → 5. **Acceptance met end to end:** picking `llama-3.1-8b-instant` wrote `explore_ai` as `{provider: groq, model: llama-3.1-8b-instant}` and the next Ask answer came back from that model. The original default (`gemini-3.5-flash-lite`) was restored afterwards and the stray test chat deleted. Ollama is absent from the provider list because it currently reports zero models — the same "a provider only counts when it lists a model" rule the web uses, not a hardcoded list. |
| 2026-08-17 | Not built in Phase 7: per-model **visibility** toggles (`enabled_models` / `enabled_providers` preferences) and the upload-limit control the web Settings also has. The picker lists everything a provider reports. Worth adding in Phase 8 for full parity. |
| 2026-08-17 | Environment note: port 8000 was taken by an unrelated `python -m http.server 8000` (PID 7007), which answers 404 to every `/api/*` call. Verification ran against a temporary API on :8010 (Telegram env vars deliberately blanked so the session could not be invalidated again); both were stopped and the app's server URL reset to the default afterwards. |

## Open items / notes

- App icon is a placeholder until Phase 8.
- Device testing needs the Mac's LAN IP as the server URL (local http; ATS exception is dev-only).
